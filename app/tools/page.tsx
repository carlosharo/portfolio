import { PTContainer } from '../_commons/_components';
import { CountdownRedux } from './components/countdownRedux';

export default function About() {
    return (
        <PTContainer>
            <CountdownRedux />
        </PTContainer>
    );
}