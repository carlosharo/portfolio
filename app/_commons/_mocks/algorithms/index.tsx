import { length_of_last_word, remove_elements, roman_to_integer, rotate_array } from "./leetcode";

export const algorithms = [
    remove_elements,
    length_of_last_word,
    rotate_array,
    roman_to_integer,
];
